{
  "name": "st-production-house-unified",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Secure control-plane foundation for ST Production House",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "test:integration": "node --test tests/postgresIntegration.test.js",
    "migrate": "node -e \"import('./src/db/index.js').then(m => m.runMigrations(m.createPostgresAdapter())).then(r => console.log('Migrations complete:', r)).catch(e => { console.error('Migration error:', e); process.exit(1); })\"",
    "migrate:status": "node -e \"import('./src/db/index.js').then(m => m.getMigrationStatus(m.createPostgresAdapter())).then(s => console.log('Migration status:', s)).catch(e => { console.error('Status error:', e); process.exit(1); })\"",
    "verify": "node --check src/catalog/agents.js && node --check src/providers/providerRouter.js && node --check src/promotion/promotionPolicy.js && node --check src/evidence/evidenceLedger.js && node --check src/publishing/publishingService.js && node --check src/db/postgresAdapter.js && node --check src/db/migrationRunner.js && node --check src/db/index.js && node --test"
  },
  "dependencies": {
    "pg": "^8.22.0"
  }
}
