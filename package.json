{
  "name": "st-production-house-unified",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Secure control-plane foundation for ST Production House",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test",
    "verify": "node --check src/catalog/agents.js && node --check src/providers/providerRouter.js && node --check src/promotion/promotionPolicy.js && node --check src/evidence/evidenceLedger.js && node --check src/publishing/publishingService.js && node --test"
  }
}
